import { HeaderText } from '@gamepark/react-game'
import { FC } from 'react'

export const EndOfRoundPhaseNewLineupHeader: FC = () => (
  <HeaderText code="endOfRoundPhaseNewLinup" defaults={{ players: 'New lineup: Features are discarded, Premiers become Features, new movies are provided' }} />
)
